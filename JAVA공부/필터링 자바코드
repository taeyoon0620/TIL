List<String> names = Arrays.asList("Alice", "Bob", "Charlie");
names.stream()
     .filter(name -> name.startsWith("A"))
     .forEach(System.out::println);


고급 자바 코드를 원하신다면, 간단한 람다식을 사용하는 예시를 드리겠습니다:

```java
List<String> names = Arrays.asList("Alice", "Bob", "Charlie");
names.stream()
     .filter(name -> name.startsWith("A"))
     .forEach(System.out::println);
```

이 코드는 `names` 리스트에서 'A'로 시작하는 이름을 필터링하여 출력하는 스트림을 사용한 예입니다. 
람다식과 메서드 참조(`System.out::println`)가 사용된 고급 자바 코드입니다.

